---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: virtualmachines.infra.virtrigaud.io
spec:
  group: infra.virtrigaud.io
  names:
    kind: VirtualMachine
    listKind: VirtualMachineList
    plural: virtualmachines
    shortNames:
    - vm
    singular: virtualmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.providerRef.name
      name: Provider
      type: string
    - jsonPath: .spec.classRef.name
      name: Class
      type: string
    - jsonPath: .spec.imageRef.name
      name: Image
      type: string
    - jsonPath: .status.ips[*]
      name: IPs
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          VirtualMachine is the Schema for the virtualmachines API.

          spec.providerRef is locked once the VM is bound (status.id or
          status.placement.pendingHost set in the STORED object). The rule below is a
          transition rule, evaluated only on update; it lives on the root so it can
          read the stored status. With the status subresource enabled, a main-resource
          update is validated against the stored status and a status update carries
          the stored spec, so neither path can unlock or change it. providerRef is
          compared as a whole: an unset namespace equals only an unset namespace (an
          explicit "" is rejected by the ObjectRef pattern), so unset -> explicit is a
          change. (A per-field comparison through conditional expressions exceeds the
          apiserver's static CEL cost budget; whole-object equality does not.)

          spec.classRef and spec.resources are locked while a clustered create is
          pending: status.placement.pendingHost set and status.id still empty in the
          STORED object (ADR-0007 Addendum A, scheduler-accuracy amendment). The
          scheduler admitted the VM at that size and every other schedule counts the
          pending VM at it, so its owner must not grow it before the create lands.
          Once the VM is created, its size is counted from status.currentResources and
          a resize-up is admitted against the host's capacity instead. A transition
          rule on the root, like the providerRef rule above; both are compared as
          whole objects.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualMachineSpec defines the desired state of VirtualMachine.
            properties:
              classRef:
                description: ClassRef references the VMClass that defines resource
                  allocation
                properties:
                  name:
                    description: Name of the referenced object
                    maxLength: 253
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: Namespace of the referenced object (defaults to current
                      namespace)
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              disks:
                description: Disks specifies additional disks beyond the root disk
                items:
                  description: DiskSpec defines a disk configuration
                  properties:
                    expandPolicy:
                      default: Offline
                      description: ExpandPolicy defines how the disk can be expanded
                      enum:
                      - Online
                      - Offline
                      type: string
                    name:
                      description: Name is the disk identifier
                      maxLength: 63
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    scsi:
                      description: SCSI specifies SCSI controller configuration (vSphere
                        only)
                      properties:
                        controller:
                          description: |-
                            Controller specifies the SCSI controller bus number (0-3)
                            If not specified, uses the first available controller
                          format: int32
                          maximum: 3
                          minimum: 0
                          type: integer
                        controllerType:
                          default: pvscsi
                          description: ControllerType specifies the SCSI controller
                            type
                          enum:
                          - lsilogic
                          - buslogic
                          - lsilogic-sas
                          - pvscsi
                          type: string
                        sharedBus:
                          default: noSharing
                          description: SharedBus specifies the SCSI bus sharing mode
                          enum:
                          - noSharing
                          - virtualSharing
                          - physicalSharing
                          type: string
                      type: object
                    sizeGiB:
                      description: SizeGiB is the size of the disk in GiB
                      format: int32
                      maximum: 65536
                      minimum: 1
                      type: integer
                    storageClass:
                      description: StorageClass specifies the storage class (optional)
                      type: string
                    type:
                      default: thin
                      description: Type specifies the disk type (provider-specific)
                      enum:
                      - thin
                      - thick
                      - eagerzeroedthick
                      - ssd
                      - hdd
                      type: string
                  required:
                  - name
                  - sizeGiB
                  type: object
                maxItems: 20
                type: array
              imageRef:
                description: |-
                  ImageRef references the VMImage to use as base template.
                  Either ImageRef or ImportedDisk must be specified, but not both.
                properties:
                  name:
                    description: Name of the referenced object
                    maxLength: 253
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: Namespace of the referenced object (defaults to current
                      namespace)
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              importedDisk:
                description: |-
                  ImportedDisk references a pre-imported disk (e.g., from migration).
                  Either ImageRef or ImportedDisk must be specified, but not both.
                properties:
                  diskID:
                    description: |-
                      DiskID is the provider-specific disk identifier.
                      For libvirt, this is typically the volume name.
                    maxLength: 253
                    minLength: 1
                    type: string
                  format:
                    default: qcow2
                    description: Format specifies the disk format (qcow2, vmdk, raw,
                      etc.).
                    enum:
                    - qcow2
                    - vmdk
                    - raw
                    - vdi
                    - vhdx
                    type: string
                  migrationRef:
                    description: |-
                      MigrationRef references the VMMigration that imported this disk.
                      This provides traceability and audit trail for migrated disks.
                    properties:
                      name:
                        description: Name of the referenced object
                        maxLength: 253
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                    required:
                    - name
                    type: object
                  path:
                    description: |-
                      Path is the optional disk path on the provider (e.g., /var/lib/libvirt/images/disk.qcow2).
                      If not specified, the provider will determine the path based on DiskID.
                    type: string
                  sizeGiB:
                    description: |-
                      SizeGiB specifies the expected disk size in GiB.
                      Used for validation and capacity planning.
                    format: int32
                    minimum: 1
                    type: integer
                  source:
                    description: Source indicates where the disk came from.
                    enum:
                    - migration
                    - clone
                    - import
                    - snapshot
                    - manual
                    type: string
                required:
                - diskID
                type: object
              lifecycle:
                description: Lifecycle defines VM lifecycle configuration
                properties:
                  gracefulShutdownTimeout:
                    default: 60s
                    description: GracefulShutdownTimeout defines how long to wait
                      for graceful shutdown
                    type: string
                  postStart:
                    description: PostStart defines actions to take after starting
                      the VM
                    properties:
                      exec:
                        description: Exec specifies a command to execute
                        properties:
                          command:
                            description: Command is the command line to execute
                            items:
                              type: string
                            minItems: 1
                            type: array
                        required:
                        - command
                        type: object
                      httpGet:
                        description: HTTPGet specifies an HTTP GET request
                        properties:
                          host:
                            description: Host name to connect to (defaults to VM IP)
                            type: string
                          path:
                            description: Path is the HTTP path to access
                            type: string
                          port:
                            description: Port is the port to access
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          scheme:
                            default: HTTP
                            description: Scheme to use for connecting (HTTP or HTTPS)
                            enum:
                            - HTTP
                            - HTTPS
                            type: string
                        required:
                        - port
                        type: object
                      snapshot:
                        description: Snapshot specifies a snapshot to create
                        properties:
                          description:
                            description: Description provides context for the snapshot
                            type: string
                          includeMemory:
                            description: IncludeMemory indicates whether to include
                              memory state
                            type: boolean
                          name:
                            description: Name is the name hint for the snapshot
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  preStop:
                    description: PreStop defines actions to take before stopping the
                      VM
                    properties:
                      exec:
                        description: Exec specifies a command to execute
                        properties:
                          command:
                            description: Command is the command line to execute
                            items:
                              type: string
                            minItems: 1
                            type: array
                        required:
                        - command
                        type: object
                      httpGet:
                        description: HTTPGet specifies an HTTP GET request
                        properties:
                          host:
                            description: Host name to connect to (defaults to VM IP)
                            type: string
                          path:
                            description: Path is the HTTP path to access
                            type: string
                          port:
                            description: Port is the port to access
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          scheme:
                            default: HTTP
                            description: Scheme to use for connecting (HTTP or HTTPS)
                            enum:
                            - HTTP
                            - HTTPS
                            type: string
                        required:
                        - port
                        type: object
                      snapshot:
                        description: Snapshot specifies a snapshot to create
                        properties:
                          description:
                            description: Description provides context for the snapshot
                            type: string
                          includeMemory:
                            description: IncludeMemory indicates whether to include
                              memory state
                            type: boolean
                          name:
                            description: Name is the name hint for the snapshot
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                type: object
              metaData:
                description: MetaData contains cloud-init metadata configuration
                properties:
                  cloudInit:
                    description: CloudInit contains cloud-init metadata configuration
                    properties:
                      inline:
                        description: Inline contains inline cloud-init metadata in
                          YAML format
                        type: string
                      secretRef:
                        description: SecretRef references a Secret containing cloud-init
                          metadata
                        properties:
                          name:
                            description: Name of the referenced object
                            maxLength: 253
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                type: object
              networks:
                description: Networks specifies network attachments for the VM
                items:
                  description: VMNetworkRef represents a reference to a network attachment
                  properties:
                    dns:
                      description: DNS specifies DNS server IP addresses (comma-separated)
                      type: string
                    gateway:
                      description: Gateway specifies the default gateway IP address
                      pattern: ^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$
                      type: string
                    ipAddress:
                      description: IPAddress specifies a static IP address (optional)
                      pattern: ^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$
                      type: string
                    macAddress:
                      description: MACAddress specifies a static MAC address (optional)
                      pattern: ^([0-9A-Fa-f]{2}[:-]){5}([0-9A-Fa-f]{2})$
                      type: string
                    name:
                      description: Name is the name of this network attachment
                      maxLength: 63
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    networkRef:
                      description: |-
                        NetworkRef references the VMNetworkAttachment (optional)
                        When not specified, the template's pre-configured network adapter is used.
                      properties:
                        name:
                          description: Name of the referenced object
                          maxLength: 253
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        namespace:
                          description: Namespace of the referenced object (defaults
                            to current namespace)
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - name
                      type: object
                    prefix:
                      description: Prefix specifies the network prefix length (e.g.,
                        24 for /24)
                      format: int32
                      maximum: 32
                      minimum: 1
                      type: integer
                  required:
                  - name
                  type: object
                maxItems: 10
                type: array
              placement:
                description: Placement provides hints for VM placement
                properties:
                  cluster:
                    description: Cluster specifies the target cluster
                    type: string
                  datastore:
                    description: |-
                      Datastore specifies the target datastore.
                      Mutually exclusive with StoragePod; Datastore takes precedence if both are set.
                    type: string
                  folder:
                    description: Folder specifies the target folder
                    type: string
                  host:
                    description: Host specifies the target host
                    type: string
                  resourcePool:
                    description: ResourcePool specifies the target resource pool
                    type: string
                  storagePod:
                    description: |-
                      StoragePod specifies a vSphere Datastore Cluster (StoragePod) to use for automatic
                      datastore selection. The provider will pick the datastore within the cluster that
                      has the most free space. Ignored when Datastore is also set.
                    type: string
                type: object
              placementRef:
                description: PlacementRef references a VMPlacementPolicy for advanced
                  placement rules
                properties:
                  name:
                    description: Name of the referenced object
                    maxLength: 253
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              powerState:
                description: PowerState specifies the desired power state
                enum:
                - "On"
                - "Off"
                - OffGraceful
                type: string
              providerRef:
                description: |-
                  ProviderRef references the Provider that manages this VM. On a
                  VirtualMachine it is immutable once the VM is bound to a hypervisor VM
                  (status.id or status.placement.pendingHost is set): every per-VM call
                  addresses the VM by its provider id, which is meaningful only on the
                  Provider that assigned it. Before binding (e.g. to fix a typo after a
                  failed create) it may still be changed. An unset namespace means the VM's
                  own; changing it from unset to an explicit namespace also counts as a
                  change.
                properties:
                  name:
                    description: Name of the referenced object
                    maxLength: 253
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: Namespace of the referenced object (defaults to current
                      namespace)
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              resources:
                description: Resources allows overriding resource allocation from
                  the VMClass
                properties:
                  cpu:
                    description: CPU specifies the number of virtual CPUs
                    format: int32
                    maximum: 128
                    minimum: 1
                    type: integer
                  gpu:
                    description: GPU specifies GPU configuration
                    properties:
                      count:
                        description: Count specifies the number of GPUs to assign
                        format: int32
                        maximum: 8
                        minimum: 1
                        type: integer
                      memory:
                        description: Memory specifies GPU memory in MiB
                        format: int64
                        minimum: 512
                        type: integer
                      type:
                        description: Type specifies the GPU type (provider-specific)
                        pattern: ^[a-zA-Z0-9-_]+$
                        type: string
                    required:
                    - count
                    type: object
                  memoryMiB:
                    description: MemoryMiB specifies the amount of memory in MiB
                    format: int64
                    maximum: 1048576
                    minimum: 128
                    type: integer
                type: object
              snapshot:
                description: Snapshot defines snapshot-related operations
                properties:
                  revertToRef:
                    description: RevertToRef specifies a snapshot to revert to
                    properties:
                      name:
                        description: Name of the referenced object
                        maxLength: 253
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                    required:
                    - name
                    type: object
                type: object
              tags:
                description: Tags are applied to the VM for organization
                items:
                  type: string
                maxItems: 50
                type: array
              userData:
                description: UserData contains cloud-init configuration
                properties:
                  cloudInit:
                    description: CloudInit contains cloud-init configuration
                    properties:
                      inline:
                        description: Inline contains inline cloud-init data
                        type: string
                      secretRef:
                        description: SecretRef references a Secret containing cloud-init
                          data
                        properties:
                          name:
                            description: Name of the referenced object
                            maxLength: 253
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  ignition:
                    description: Ignition contains Ignition configuration for CoreOS/RHEL
                    properties:
                      inline:
                        description: Inline contains inline Ignition data
                        type: string
                      secretRef:
                        description: SecretRef references a Secret containing Ignition
                          data
                        properties:
                          name:
                            description: Name of the referenced object
                            maxLength: 253
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                type: object
            required:
            - classRef
            - providerRef
            type: object
          status:
            description: VirtualMachineStatus defines the observed state of VirtualMachine.
            properties:
              boundProvider:
                description: |-
                  BoundProvider records the Provider this VM was bound through: the one
                  whose hypervisor assigned status.id (or, for a clustered create in
                  flight, the one status.placement.pendingHost belongs to). The operator
                  writes it in the same status write that binds the VM (VM create, clone,
                  adoption) and, for a VM bound before this field existed, backfills it on
                  the first reconcile from spec.providerRef. While the VM is bound, the
                  operator makes no provider call for it — and its finalizer does not
                  delete through a Provider — unless spec.providerRef still resolves to
                  this Provider (same namespace and name and, when recorded, UID);
                  otherwise the VM reports Ready=False/ProviderRefMismatch. A
                  deliberately re-created Provider (new UID) is re-accepted by an
                  administrator clearing this field through the status subresource.
                properties:
                  name:
                    description: Name of the Provider.
                    maxLength: 253
                    type: string
                  namespace:
                    description: Namespace of the Provider.
                    maxLength: 63
                    type: string
                  uid:
                    description: |-
                      UID of the Provider object at bind time. Empty when it was not known
                      (never for a VM bound by this operator version). A Provider deleted and
                      re-created under the same name has a new UID and is not accepted in its
                      place.
                    maxLength: 128
                    type: string
                required:
                - name
                - namespace
                type: object
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consoleURL:
                description: ConsoleURL provides access to the VM console
                type: string
              currentResources:
                description: CurrentResources shows the current resource allocation
                properties:
                  cpu:
                    description: CPU specifies the number of virtual CPUs
                    format: int32
                    maximum: 128
                    minimum: 1
                    type: integer
                  gpu:
                    description: GPU specifies GPU configuration
                    properties:
                      count:
                        description: Count specifies the number of GPUs to assign
                        format: int32
                        maximum: 8
                        minimum: 1
                        type: integer
                      memory:
                        description: Memory specifies GPU memory in MiB
                        format: int64
                        minimum: 512
                        type: integer
                      type:
                        description: Type specifies the GPU type (provider-specific)
                        pattern: ^[a-zA-Z0-9-_]+$
                        type: string
                    required:
                    - count
                    type: object
                  memoryMiB:
                    description: MemoryMiB specifies the amount of memory in MiB
                    format: int64
                    maximum: 1048576
                    minimum: 128
                    type: integer
                type: object
              id:
                description: ID is the provider-specific identifier for this VM
                type: string
              ips:
                description: IPs contains the IP addresses assigned to the VM
                items:
                  type: string
                type: array
              lastReconfigureTime:
                description: LastReconfigureTime records when the last reconfiguration
                  occurred
                format: date-time
                type: string
              lastTaskRef:
                description: LastTaskRef references the last async operation
                type: string
              message:
                description: Message provides additional details about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the VM
                enum:
                - Pending
                - Provisioning
                - Running
                - Stopped
                - Reconfiguring
                - Deleting
                - Failed
                type: string
              placement:
                description: |-
                  Placement records where a clustered ("brain-in-operator") provider's
                  scheduler bound this VM (ADR-0007 P1, D3). It is the durable source of
                  truth for which host runs the VM. It is written by the operator ONLY after
                  the provider confirms the VM is on that host (create success / migration
                  done) — never speculatively (honesty-first). Empty for VMs on single-host
                  or thin-client providers, which have no operator-owned placement decision.
                properties:
                  excludedHosts:
                    description: |-
                      ExcludedHosts are Hosts (CR names) the scheduler must not choose for this
                      VM (ADR-0007 Addendum A, A2 amendment). A Host is added when a Create on
                      it was refused with a name conflict (ALREADY_EXISTS: a same-named domain
                      this VM does not own is there), which proves this VM has no domain on it
                      and that this attempt created nothing there (an earlier failed attempt
                      may have left disk or cloud-init files, which no owner-checked cleanup
                      could remove either); its pendingHost is then cleared and the VM is
                      re-scheduled onto another Host. The list is bounded (the oldest entry is
                      dropped when it is full) and is cleared when the VM is bound. If every
                      candidate Host is
                      excluded the VM stays unplaced (Placed=False/AllHostsExcluded) until an
                      administrator resolves the name conflicts and clears this list.
                    items:
                      maxLength: 253
                      type: string
                    maxItems: 16
                    type: array
                    x-kubernetes-list-type: set
                  host:
                    description: |-
                      Host is the Host (CR name) this VM is bound to run on — the durable
                      placement source of truth (D3). Written only after the provider confirms.
                      Every per-VM provider call for the VM is routed to this host.
                    type: string
                  lastScheduledTime:
                    description: LastScheduledTime is when the scheduler last (re)bound
                      this VM to Host.
                    format: date-time
                    type: string
                  memoryCeilingMiB:
                    description: |-
                      MemoryCeilingMiB is the memory the VM's domain may reach on its host
                      beyond its current allocation, recorded when its create was scheduled
                      (ADR-0007 Addendum A, scheduler-accuracy amendment): the balloon maximum
                      provisioned for a VMClass with memory hot-add (4× the initial memory), or
                      0 when none was provisioned. A guest can deflate its balloon up to it, so
                      the clustered accounting counts the VM's memory at the larger of this and
                      its current size. It is kept when the VM is bound; unset means unknown (a
                      VM scheduled by an older manager), and the VMClass's hot-add setting is
                      used instead.
                    format: int64
                    minimum: 0
                    type: integer
                  pendingHost:
                    description: |-
                      PendingHost is the Host (CR name) an in-flight, not yet confirmed Create is
                      aimed at (ADR-0007 Addendum A, A2). The operator writes it, with Pool,
                      through a resourceVersion-checked status update BEFORE calling Create, so
                      a retry after a lost write or a Create that ran past its deadline lands on
                      the SAME host instead of being scheduled onto another one (which could
                      leave a second domain of the same name behind). When Create succeeds it is
                      moved into Host and cleared. It is used only to route the Create retry and
                      the finalizer's owner-checked cleanup; it is never a binding. An
                      administrator may clear it to release a VM whose pending host is gone for
                      good (the operator never re-schedules such a VM on its own).
                    type: string
                  pendingResources:
                    description: |-
                      PendingResources is the size the scheduler admitted a pending Create at
                      (ADR-0007 Addendum A, scheduler-accuracy amendment). The operator writes
                      it with PendingHost, in the same checked status update, and clears it when
                      the VM is bound or the pending host is released. While a create is
                      pending, the VM counts at this size against its host's capacity, and a
                      retry of the Create whose size has grown beyond it (its VMClass was
                      changed in the meantime) is not sent.
                    properties:
                      cpu:
                        description: CPU is the number of vCPUs.
                        format: int32
                        minimum: 0
                        type: integer
                      memoryMiB:
                        description: MemoryMiB is the memory in MiB.
                        format: int64
                        minimum: 0
                        type: integer
                    required:
                    - cpu
                    - memoryMiB
                    type: object
                  pool:
                    description: Pool is the HostPool the VM was scheduled into.
                    type: string
                  reason:
                    description: |-
                      Reason records why this host was chosen — the scheduler's decision trace
                      (e.g. the winning score or the constraint that narrowed the candidates).
                    type: string
                type: object
              powerState:
                description: PowerState reflects the current power state
                enum:
                - "On"
                - "Off"
                - OffGraceful
                type: string
              provider:
                additionalProperties:
                  type: string
                description: Provider contains provider-specific details
                type: object
                x-kubernetes-preserve-unknown-fields: true
              reconfigureTaskRef:
                description: ReconfigureTaskRef tracks reconfiguration operations
                type: string
              snapshots:
                description: Snapshots lists available snapshots for this VM
                items:
                  description: VMSnapshotInfo provides information about a VM snapshot
                  properties:
                    creationTime:
                      description: CreationTime is when the snapshot was created
                      format: date-time
                      type: string
                    description:
                      description: Description provides additional context
                      type: string
                    hasMemory:
                      description: HasMemory indicates if memory state is included
                      type: boolean
                    id:
                      description: ID is the provider-specific snapshot identifier
                      type: string
                    name:
                      description: Name is the snapshot name
                      type: string
                    sizeBytes:
                      description: SizeBytes is the size of the snapshot
                      format: int64
                      type: integer
                  required:
                  - creationTime
                  - id
                  - name
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - fieldPath: .spec.providerRef
          message: spec.providerRef is immutable once the VirtualMachine is bound
            (status.id or status.placement.pendingHost is set); to stop managing the
            VM without destroying it, annotate it virtrigaud.io/orphan-on-delete=true
            and delete it
          rule: '!has(oldSelf.status) || ((!has(oldSelf.status.id) || size(oldSelf.status.id)
            == 0) && (!has(oldSelf.status.placement) || !has(oldSelf.status.placement.pendingHost)
            || size(oldSelf.status.placement.pendingHost) == 0)) || (has(self.spec)
            && has(oldSelf.spec) && self.spec.providerRef == oldSelf.spec.providerRef)'
        - fieldPath: .spec
          message: 'spec.classRef and spec.resources are immutable while a clustered
            create is pending (status.placement.pendingHost is set and status.id is
            empty): the VM was scheduled at this size; change them once the VM is
            created'
          rule: '!has(oldSelf.status) || !has(oldSelf.status.placement) || !has(oldSelf.status.placement.pendingHost)
            || size(oldSelf.status.placement.pendingHost) == 0 || (has(oldSelf.status.id)
            && size(oldSelf.status.id) > 0) || (has(self.spec) && has(oldSelf.spec)
            && self.spec.classRef == oldSelf.spec.classRef && has(self.spec.resources)
            == has(oldSelf.spec.resources) && (!has(self.spec.resources) || self.spec.resources
            == oldSelf.spec.resources))'
    served: true
    storage: true
    subresources:
      scale:
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
