---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: vmimages.infra.virtrigaud.io.infra.virtrigaud.io
spec:
  group: infra.virtrigaud.io.infra.virtrigaud.io
  names:
    kind: VMImage
    listKind: VMImageList
    plural: vmimages
    singular: vmimage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.availableOn
      name: Providers
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VMImage is the Schema for the vmimages API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VMImageSpec defines the desired state of VMImage.
            properties:
              libvirt:
                description: Libvirt contains Libvirt-specific image configuration
                properties:
                  checksum:
                    description: Checksum provides expected checksum for verification
                    type: string
                  checksumType:
                    default: sha256
                    description: ChecksumType specifies the checksum algorithm
                    enum:
                    - md5
                    - sha1
                    - sha256
                    - sha512
                    type: string
                  format:
                    default: qcow2
                    description: Format specifies the image format
                    enum:
                    - qcow2
                    - raw
                    - vmdk
                    type: string
                  path:
                    description: Path specifies the path to the image file
                    type: string
                  url:
                    description: URL provides a URL to download the image
                    type: string
                type: object
              prepare:
                description: Prepare contains optional image preparation steps
                properties:
                  force:
                    description: Force forces re-import even if image exists
                    type: boolean
                  onMissing:
                    allOf:
                    - enum:
                      - Import
                      - Fail
                    - enum:
                      - Import
                      - Fail
                    default: Import
                    description: OnMissing defines the action to take when image is
                      missing
                    type: string
                  retries:
                    default: 3
                    description: Retries defines the number of retry attempts for
                      failed operations
                    format: int32
                    type: integer
                  storage:
                    description: Storage defines storage-specific preparation options
                    properties:
                      libvirt:
                        description: Libvirt storage options
                        properties:
                          allocationPolicy:
                            description: AllocationPolicy defines how storage is allocated
                            enum:
                            - eager
                            - lazy
                            type: string
                          storagePool:
                            description: StoragePool specifies the target storage
                              pool for import
                            type: string
                        type: object
                      vsphere:
                        description: VSphere storage options
                        properties:
                          datastore:
                            description: Datastore specifies the target datastore
                              for import
                            type: string
                          folder:
                            description: Folder specifies the target folder for import
                            type: string
                          thinProvisioned:
                            description: ThinProvisioned indicates whether to use
                              thin provisioning
                            type: boolean
                        type: object
                    type: object
                  timeout:
                    default: 30m
                    description: Timeout defines the maximum time to wait for preparation
                    type: string
                  validateChecksum:
                    default: true
                    description: ValidateChecksum validates the image checksum
                    type: boolean
                type: object
              vsphere:
                description: VSphere contains vSphere-specific image configuration
                properties:
                  checksum:
                    description: Checksum provides expected checksum for verification
                    type: string
                  checksumType:
                    default: sha256
                    description: ChecksumType specifies the checksum algorithm
                    enum:
                    - md5
                    - sha1
                    - sha256
                    - sha512
                    type: string
                  ovaURL:
                    description: OVAURL provides a URL to an OVA file to import
                    type: string
                  templateName:
                    description: TemplateName references an existing vSphere template
                    type: string
                type: object
            type: object
          status:
            description: VMImageStatus defines the observed state of VMImage.
            properties:
              availableOn:
                description: AvailableOn lists the providers where the image is available
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              importProgress:
                description: ImportProgress shows the progress of image import operations
                properties:
                  eta:
                    description: ETA is the estimated time to completion
                    type: string
                  percentage:
                    description: Percentage is the completion percentage (0-100)
                    format: int32
                    type: integer
                  totalBytes:
                    description: TotalBytes is the total size of the image being imported
                    format: int64
                    type: integer
                  transferRate:
                    description: TransferRate is the current transfer rate in bytes
                      per second
                    format: int64
                    type: integer
                  transferredBytes:
                    description: TransferredBytes is the number of bytes transferred
                      so far
                    format: int64
                    type: integer
                type: object
              lastPrepareTime:
                description: LastPrepareTime records when the image was last prepared
                format: date-time
                type: string
              message:
                description: Message provides additional details about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of image preparation
                enum:
                - Pending
                - Importing
                - Preparing
                - Ready
                - Failed
                type: string
              prepareTaskRef:
                description: PrepareTaskRef tracks any ongoing image preparation operations
                type: string
              ready:
                description: Ready indicates if the image is ready for use
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
