{{- if .Values.providers.proxmox.enabled }}
{{- if .Values.providers.proxmox.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "virtrigaud.fullname" . }}-provider-proxmox
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "virtrigaud.labels" . | nindent 4 }}
    app.kubernetes.io/component: provider-proxmox
spec:
  replicas: {{ .Values.providers.proxmox.remote.replicaCount }}
  selector:
    matchLabels:
      {{- include "virtrigaud.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: provider-proxmox
  template:
    metadata:
      labels:
        {{- include "virtrigaud.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: provider-proxmox
    spec:
      serviceAccountName: {{ include "virtrigaud.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.manager.podSecurityContext | nindent 8 }}
      containers:
      - name: provider-proxmox
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.providers.proxmox.image.repository }}:{{ .Values.providers.proxmox.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        securityContext:
          {{- toYaml .Values.manager.securityContext | nindent 10 }}
        command:
        - /usr/local/bin/provider-proxmox
        args:
        - --port=9090
        - --health-port=8080
        {{- if .Values.providers.proxmox.env }}
        env:
        {{- range .Values.providers.proxmox.env }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        {{- end }}
        ports:
        - containerPort: 9090
          name: grpc
          protocol: TCP
        - containerPort: 8080
          name: health
          protocol: TCP
        livenessProbe:
          grpc:
            port: 9090
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          grpc:
            port: 9090
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          {{- toYaml .Values.providers.proxmox.remote.resources | nindent 10 }}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
      {{- with .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
{{- end }}
