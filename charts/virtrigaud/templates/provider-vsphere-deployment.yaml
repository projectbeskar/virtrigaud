{{- if .Values.providers.vsphere.enabled }}
{{- if .Values.providers.vsphere.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "virtrigaud.fullname" . }}-provider-vsphere
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "virtrigaud.labels" . | nindent 4 }}
    app.kubernetes.io/component: provider-vsphere
spec:
  replicas: {{ .Values.providers.vsphere.replicaCount }}
  selector:
    matchLabels:
      {{- include "virtrigaud.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: provider-vsphere
  template:
    metadata:
      labels:
        {{- include "virtrigaud.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: provider-vsphere
    spec:
      serviceAccountName: {{ include "virtrigaud.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.manager.podSecurityContext | nindent 8 }}
      containers:
      - name: provider-vsphere
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.providers.vsphere.image.repository }}:{{ .Values.providers.vsphere.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        securityContext:
          {{- toYaml .Values.manager.securityContext | nindent 10 }}
        command:
        - /usr/local/bin/provider-vsphere
        args:
        - --port=9090
        - --health-port=8080
        ports:
        - containerPort: 9090
          name: grpc
          protocol: TCP
        - containerPort: 8080
          name: health
          protocol: TCP
        livenessProbe:
          grpc:
            port: 9090
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          grpc:
            port: 9090
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          {{- toYaml .Values.providers.vsphere.resources | nindent 10 }}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
      {{- with .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
{{- end }}
