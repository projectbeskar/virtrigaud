---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: vmclasses.infra.virtrigaud.io
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: virtrigaud-webhook
          namespace: virtrigaud
          path: /convert
      conversionReviewVersions:
      - v1
  group: infra.virtrigaud.io
  names:
    kind: VMClass
    listKind: VMClassList
    plural: vmclasses
    shortNames:
    - vmclass
    singular: vmclass
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cpu
      name: CPU
      type: integer
    - jsonPath: .spec.memoryMiB
      name: Memory
      type: string
    - jsonPath: .spec.firmware
      name: Firmware
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VMClass is the Schema for the vmclasses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VMClassSpec defines the desired state of VMClass.
            properties:
              cpu:
                description: CPU specifies the number of virtual CPUs
                format: int32
                type: integer
              diskDefaults:
                description: DiskDefaults provides default disk settings
                properties:
                  sizeGiB:
                    default: 40
                    description: SizeGiB specifies the default root disk size in GiB
                    format: int32
                    type: integer
                  type:
                    default: thin
                    description: Type specifies the default disk type
                    enum:
                    - thin
                    - thick
                    - eagerzeroedthick
                    type: string
                type: object
              extraConfig:
                additionalProperties:
                  type: string
                description: ExtraConfig contains provider-specific extra configuration
                type: object
              firmware:
                default: BIOS
                description: Firmware specifies the firmware type
                enum:
                - BIOS
                - UEFI
                type: string
              guestToolsPolicy:
                default: install
                description: GuestToolsPolicy specifies guest tools installation policy
                enum:
                - install
                - skip
                - upgrade
                type: string
              memoryMiB:
                description: MemoryMiB specifies memory in MiB
                format: int32
                type: integer
            required:
            - cpu
            - memoryMiB
            type: object
          status:
            description: VMClassStatus defines the observed state of VMClass.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.cpu
      name: CPU
      type: integer
    - jsonPath: .spec.memory
      name: Memory
      type: string
    - jsonPath: .spec.firmware
      name: Firmware
      type: string
    - jsonPath: .status.usedByVMs
      name: Used By
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VMClass is the Schema for the vmclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VMClassSpec defines the desired state of VMClass
            properties:
              cpu:
                description: CPU specifies the number of virtual CPUs
                format: int32
                maximum: 128
                minimum: 1
                type: integer
              diskDefaults:
                description: DiskDefaults provides default disk settings
                properties:
                  iops:
                    description: IOPS specifies the default IOPS limit
                    format: int32
                    maximum: 100000
                    minimum: 100
                    type: integer
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 40Gi
                    description: Size specifies the default root disk size
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClass:
                    description: StorageClass specifies the default storage class
                    maxLength: 253
                    type: string
                  type:
                    default: thin
                    description: Type specifies the default disk type
                    enum:
                    - thin
                    - thick
                    - eagerzeroedthick
                    - ssd
                    - hdd
                    - nvme
                    type: string
                type: object
              extraConfig:
                additionalProperties:
                  type: string
                description: ExtraConfig contains provider-specific extra configuration
                maxProperties: 50
                type: object
              firmware:
                allOf:
                - enum:
                  - BIOS
                  - UEFI
                  - EFI
                - enum:
                  - BIOS
                  - UEFI
                  - EFI
                default: BIOS
                description: Firmware specifies the firmware type
                type: string
              guestToolsPolicy:
                default: install
                description: GuestToolsPolicy specifies guest tools installation policy
                enum:
                - install
                - skip
                - upgrade
                - uninstall
                type: string
              memory:
                anyOf:
                - type: integer
                - type: string
                description: Memory specifies memory allocation using Kubernetes resource
                  quantities
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              performanceProfile:
                description: PerformanceProfile defines performance-related settings
                properties:
                  cpuHotAddEnabled:
                    default: false
                    description: CPUHotAddEnabled allows adding CPUs while VM is running
                    type: boolean
                  hyperThreadingPolicy:
                    default: auto
                    description: HyperThreadingPolicy controls hyperthreading usage
                    enum:
                    - auto
                    - prefer
                    - avoid
                    - require
                    type: string
                  latencySensitivity:
                    default: normal
                    description: LatencySensitivity configures latency sensitivity
                    enum:
                    - low
                    - normal
                    - high
                    type: string
                  memoryHotAddEnabled:
                    default: false
                    description: MemoryHotAddEnabled allows adding memory while VM
                      is running
                    type: boolean
                  nestedVirtualization:
                    default: false
                    description: NestedVirtualization enables nested virtualization
                    type: boolean
                  virtualizationBasedSecurity:
                    default: false
                    description: VirtualizationBasedSecurity enables VBS features
                    type: boolean
                type: object
              resourceLimits:
                description: ResourceLimits defines resource limits and reservations
                properties:
                  cpuLimit:
                    description: CPULimit is the maximum CPU usage limit (in MHz or
                      percentage)
                    format: int32
                    maximum: 100000
                    minimum: 100
                    type: integer
                  cpuReservation:
                    description: CPUReservation is the guaranteed CPU allocation (in
                      MHz)
                    format: int32
                    maximum: 100000
                    minimum: 0
                    type: integer
                  cpuShares:
                    description: CPUShares defines the relative CPU priority (higher
                      = more priority)
                    format: int32
                    maximum: 1000000
                    minimum: 1
                    type: integer
                  memoryLimit:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MemoryLimit is the maximum memory usage limit
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  memoryReservation:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MemoryReservation is the guaranteed memory allocation
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              securityProfile:
                description: SecurityProfile defines security-related settings
                properties:
                  encryptionPolicy:
                    description: EncryptionPolicy defines VM encryption settings
                    properties:
                      enabled:
                        default: false
                        description: Enabled indicates if encryption should be used
                        type: boolean
                      keyProvider:
                        description: KeyProvider specifies the encryption key provider
                        enum:
                        - standard
                        - hardware
                        - external
                        type: string
                      requireEncryption:
                        default: false
                        description: RequireEncryption mandates encryption (fails
                          if not available)
                        type: boolean
                    type: object
                  secureBoot:
                    default: false
                    description: SecureBoot enables secure boot functionality
                    type: boolean
                  tpmEnabled:
                    default: false
                    description: TPMEnabled enables TPM (Trusted Platform Module)
                    type: boolean
                  tpmVersion:
                    description: TPMVersion specifies the TPM version
                    enum:
                    - 1.2
                    - 2
                    type: string
                  vtdEnabled:
                    default: false
                    description: VTDEnabled enables Intel VT-d or AMD-Vi
                    type: boolean
                type: object
            required:
            - cpu
            - memory
            type: object
          status:
            description: VMClassStatus defines the observed state of VMClass
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              supportedProviders:
                description: SupportedProviders lists the providers that support this
                  class
                items:
                  type: string
                type: array
              usedByVMs:
                description: UsedByVMs is the number of VMs currently using this class
                format: int32
                type: integer
              validationResults:
                additionalProperties:
                  description: ValidationResult represents a validation result for
                    a provider
                  properties:
                    lastValidated:
                      description: LastValidated is when this validation was last
                        performed
                      format: date-time
                      type: string
                    message:
                      description: Message provides details about the validation result
                      type: string
                    valid:
                      description: Valid indicates if the class is valid for the provider
                      type: boolean
                    warnings:
                      description: Warnings lists any validation warnings
                      items:
                        type: string
                      type: array
                  required:
                  - valid
                  type: object
                description: ValidationResults contains validation results for different
                  providers
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
